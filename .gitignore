.DS_Store
Thumbs.db
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Inno Setup installer output
/installer/dist/

# MSIX build artifacts (signed .msix, staged package, private .pfx)
/installer/msix/dist/
/installer/msix/stage/
/installer/msix/.cert/

# Defensive: never commit signing keys or local debug logs anywhere
*.pfx
*.p12
*.key
*.log

# Internal-only docs and editor/agent configs not for public consumption
/DESIGN.md
/.claude/
# Top-level examples/ is a working copy of the demo videos.
# The runtime serves them from static/examples/ — keep that, not this.
/examples/
